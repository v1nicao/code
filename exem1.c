#include <stdio.h>
 
int main () {
 
    int num1, num2, num3, num4, num5;
    int maior, menor, num, mainum, mennum;
 
   
    for(num == 0; num < 10; num++;) {
        scanf("%d %d %d %d %d", &num1, &num2, &num3, &num4, &num5);
        if(num > maior || num == 0) maior = mainum;
        if(num < menor || num == 0) menor = mennum;
    }
    printf("MENOR: %d, MAIOR: %d\n", mainum, mennum);
    printf("DESORDENADO\n");
 
    return 0;
}